<script>
    // redirect access token to appropriate url
    if (window.location.hash) {
        var cookie = document.cookie;
        var app = cookie.split('=')[1];
        window.location = "/" + app + window.location.hash;
    }
</script>

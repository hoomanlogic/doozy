var React = require('react');
var ReactDOM = require('react-dom');
var PlanStepsInterface = require('interfaces/PlanSteps');
if (window) {
    window.React = React;
    window.ReactDOM = ReactDOM;
    window.MountInterface = PlanStepsInterface;
}

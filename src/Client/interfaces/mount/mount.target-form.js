var React = require('react');
var ReactDOM = require('react-dom');
var TargetFormInterface = require('interfaces/TargetForm');
if (window) {
    window.React = React;
    window.ReactDOM = ReactDOM;
    window.MountInterface = TargetFormInterface;
}

var React = require('react');
var ReactDOM = require('react-dom');
var TagFormInterface = require('interfaces/TagForm');
if (window) {
    window.React = React;
    window.ReactDOM = ReactDOM;
    window.MountInterface = TagFormInterface;
}

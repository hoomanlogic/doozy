var React = require('react');
var ReactDOM = require('react-dom');
var FocusFormInterface = require('interfaces/FocusForm');
if (window) {
    window.React = React;
    window.ReactDOM = ReactDOM;
    window.MountInterface = FocusFormInterface;
}

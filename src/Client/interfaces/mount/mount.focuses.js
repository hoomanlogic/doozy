var React = require('react');
var ReactDOM = require('react-dom');
var FocusesInterface = require('interfaces/Focuses');
if (window) {
    window.React = React;
    window.ReactDOM = ReactDOM;
    window.MountInterface = FocusesInterface;
}

var React = require('react');
var ReactDOM = require('react-dom');
var PlanStepFormInterface = require('interfaces/PlanStepForm');
if (window) {
    window.React = React;
    window.ReactDOM = ReactDOM;
    window.MountInterface = PlanStepFormInterface;
}

var React = require('react');
var ReactDOM = require('react-dom');
var PlanFormInterface = require('interfaces/PlanForm');
if (window) {
    window.React = React;
    window.ReactDOM = ReactDOM;
    window.MountInterface = PlanFormInterface;
}
